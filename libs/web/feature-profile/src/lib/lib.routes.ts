import { Route } from '@angular/router';
import { FeatureProfile } from './feature-profile/feature-profile';

export const featureProfileRoutes: Route[] = [
  { path: '', component: FeatureProfile },
];
