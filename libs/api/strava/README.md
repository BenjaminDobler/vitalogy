# strava

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build strava` to build the library.

## Running unit tests

Run `nx test strava` to execute the unit tests via [Jest](https://jestjs.io).
