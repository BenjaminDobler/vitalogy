import { Route } from '@angular/router';
import { FeatureHistory } from './feature-history/feature-history';

export const featureHistoryRoutes: Route[] = [
  { path: '', component: FeatureHistory },
];
